
[RENDER]
If using Render, Dockerfile can be reused, just rename the project name.



@ = When you want to direct a url away from the source.
- Example when we deploy a program http://localhost:8080/cart to Railway, if we use @, we will replace the url generated by Railway.
- Replace your current host url with another host url.

$ = to reference back to the variable that you added to the model

* = used with $

# = to ask Thymeleaf to use Java API, aka using Java code in html